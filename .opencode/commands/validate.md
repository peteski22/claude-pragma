---
description: Run all validators against recent code changes
agent: build
---

Load the `validate` skill using the skill tool, then follow its instructions to run all applicable validators against recent code changes and report results.
